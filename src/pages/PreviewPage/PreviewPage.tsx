import { Container } from '@chakra-ui/react';
import { Preview } from 'src/widgets/Preview';

const PreviewPage = () => (
  <Container maxW={'100%'}>
    <Preview />
  </Container>
);

export { PreviewPage };
